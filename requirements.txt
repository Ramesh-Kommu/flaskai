Flask==3.1.0            # Flask framework for the web application
Flask-Cors==2.1.2          # To handle Cross-Origin Resource Sharing (CORS) for your API
requests==2.31.0         # For making HTTP requests (e.g., for interacting with Gemini AI)
httpx==0.23.0            # For async HTTP requests (ensure compatibility with other dependencies)
gunicorn==20.1.0         # WSGI HTTP server for deploying Flask app in production
sqlite3                  # For SQLite database (SQLite is built-in with Python, but some environments need this)
